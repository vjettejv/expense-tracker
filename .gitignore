# Bỏ qua thư mục thư viện (nặng)
/vendor/

# Bỏ qua file cấu hình chứa mật khẩu (quan trọng)
.env
config.php

# Bỏ qua file rác của máy tính
.DS_Store
Thumbs.db

# Bỏ qua ảnh người dùng upload (chỉ giữ lại thư mục rỗng)
/uploads/*
!/uploads/.gitkeep
